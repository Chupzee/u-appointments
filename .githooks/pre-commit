#!/bin/sh
branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "✋ Direct commits to $branch are blocked. Create/switch to a feature branch."
  exit 1
fi
